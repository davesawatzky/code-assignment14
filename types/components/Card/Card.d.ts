/// <reference types="react" />
import { CardProps } from './Card.types';
declare function Card({ cardText, cardHeadingText, cardSubText, id, error, disabled, ...props }: CardProps): JSX.Element;
export default Card;
