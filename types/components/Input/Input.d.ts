/// <reference types="react" />
import { InputProps } from './Input.types';
declare function Input({ id, name, disabled, label, message, error, success, onChange, placeholder, ...props }: InputProps): JSX.Element;
export default Input;
